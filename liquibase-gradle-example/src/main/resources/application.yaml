spring:
  datasource:
    url: jdbc:postgresql://localhost/paste
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true