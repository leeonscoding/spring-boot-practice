spring:
  datasource:
    url: jdbc:postgresql://localhost/employee1
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true